# Docmind Desktop — Build Configuration
# Copy this file to .env and fill in the values.

# HMAC secret for license key validation (required for production builds)
# Generate with: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
DOCMIND_LICENSE_SECRET=your-secret-here
